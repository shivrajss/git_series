
Gir init --- initiliing the git repository
git clone -- to download from remote repository
Git status -- initilizing the git status 
git config color.ui true -- Changing the color in Git repository
vi notes.txt --- add the content to the text window
---

git add [file name] -- take the file and add it to the basket
git add . --- to add all the file names together without initilizing with the file name
git commit -- to add the message to the changes or
git commit -m "message" ---
   -- if we get any warning messages while doing the commit activiy use the following commands
git config --local user.name Shivaraj S
git config --local user.email shivar@qti.qualcomm.com

git log -- print all the commit done 
